package test:caller;


interface api {

  record new-type {
    value: string
  }

  record another-type {
    value: string
  }

  run: func(name: string) -> new-type;
}

world caller {
  
  export api;
}
